# This directory previously contained the SalesTransaction POJO.
# We now use Protocol Buffers for the data model.
# The generated classes are created from protos/sales_transaction.proto
# during the build process.
